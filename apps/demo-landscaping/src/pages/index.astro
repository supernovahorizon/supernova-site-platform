---
import { createLocalBusinessJsonLd } from '@supernova/structured-data';
import LandscapingHomePage from '@supernova/ui-sections/LandscapingHomePage.astro';

import LandscapingLayout from '../layouts/LandscapingLayout.astro';
import { site } from '../site';

const jsonLd = createLocalBusinessJsonLd({
  business: site.business,
  siteUrl: site.siteUrl,
});
---

<LandscapingLayout
  title="Home"
  description={site.seo.defaultDescription}
  path="/"
  jsonLd={jsonLd}
>
  <LandscapingHomePage site={site} />
</LandscapingLayout>